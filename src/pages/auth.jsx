import React from "react";
import Login from "../auth/login";

export default function Auth(props){
    //Login,
    //Check if 
    return (
        <div class="main_container center_center_vertical">
            <Login set_views={props.set_views} />
        </div>
    )
}